# At least 16 characters; must differ from the storyboard profile key.
API_SERVER_KEY=
SCRIPTMAKER_API_BASE=
# Shared New API token for turns that carry no per-user key.
SCRIPTMAKER_MODEL_API_KEY=
# Platform-owned New API token used only by background skill review, so a
# review is never billed to the user whose turn happened to trigger it.
SCRIPTMAKER_REVIEW_API_KEY=
